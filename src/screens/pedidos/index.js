import { Text, View, SafeAreaView, StatusBar, ScrollView, TouchableOpacity, Image } from 'react-native';

function Pedidos() {
  return (
    <SafeAreaView>      
      <StatusBar style="dark" backgroundColor='#f95e56' />
    </SafeAreaView>
  );
} 

export default Pedidos;

